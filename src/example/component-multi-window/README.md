# MultiWindow

Multiple window component demo. Creates, destroys, and resets windows from the component registry via `createComponent`.

## Features

- Launch multiple window types from registry
- Independent window lifecycle (create / destroy / reset)
- Overlapping draggable windows
- Per-window content isolation
