# _shared/

多个 example 共用的工具函数/组件。

## 文件

| 文件 | 职责 |
|------|------|
| `Displays.ts` | `mountDisplays(sc)` — 给 SubCanvas region 挂载 crosshair + click ring visualizer。用于 `#single` 和 `#multiple` 路由。 |
