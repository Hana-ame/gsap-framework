# BackendControlledDisplay

Server-driven UI demo. Backend controls window content, layout, and lifecycle via ContentChannel.

## Features

- Backend-pushed window creation and destruction
- Content streaming through typed channel
- Reactive UI updates from mock server events
