# ComponentVideoPlayerDomDisplay

纯 React DOM `<video>` 封装。无 PIXI、无 SubCanvas、零 PIXI 视频坑。

## 演示

- `VideoPlayer` React 组件（forwardRef + `<video controls>`）
- 外部 play/pause/toggle 按钮
- 与 PIXI canvas 共存，但视频元素在 DOM 层
- 对比 `component-video-player` 展示 DOM 方案 vs PIXI 方案的取舍
