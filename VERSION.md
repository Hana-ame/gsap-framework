# 版本更新与开发日志 (VERSION.md)

## 文件说明

### 用途

本文档用于记录本项目（Pixi.js React 测试框架）的所有重要变更、开发过程中遇到的困难、解决方案以及每个版本的功能状态。它是项目的“活历史”，帮助开发者理解设计决策和演进脉络。

### 结构

- **版本条目**：按时间倒序排列，每个版本包含版本号、发布日期、更新内容、困难与修正、功能列表、测试验证和待办事项。
- **困难与修正**：详细描述开发中遇到的典型问题（技术难点、设计缺陷等）以及最终的解决方案，为后续类似问题提供参考。

### 使用方法

- 每次完成一个功能迭代或修复重要问题后，在此文件的顶部（`Unreleased` 区域或新版本）添加变更记录。
- 描述更新内容时，应清晰区分“新增”、“修改”、“修复”、“移除”等类型。
- “困难与修正”部分应包含问题背景、尝试的解决方案、最终选定的方案及其理由。

### 注意事项

- 版本号遵循 [语义化版本 2.0.0](https://semver.org/lang/zh-CN/)。
- 所有日期采用 ISO 格式（YYYY-MM-DD）。
- 保持语言简洁、客观，避免主观评价。

## [Unreleased]

### 计划中功能

- **WebSocket 自动消息映射**：将 WebSocket 接收到的消息直接路由到对应插件，无需手动调用 `controller.sendToPixi`。
- **插件热加载**：允许在运行时动态注册/注销插件（需评估 Pixi 应用状态管理）。
- **更多图形插件**：如文本、线条、图片插件，丰富绘图能力。
- **单元测试**：为核心控制器和插件编写 Jest 测试用例。

## [0.7.0] - 2026-02-28


### 核心更新

- **新增 `GameController` 业务控制器**：将事件监听、日志更新、鼠标转发等所有业务逻辑从 `App.tsx` 中抽离，使 UI 层仅负责渲染和用户输入。`GameController` 提供简洁的 API（如 `drawCircle()`、`startBalls()`）供按钮调用，并内部处理从画布收到的事件。
- **重构物理插件为模块化结构**：将原有的 `bouncePlugin` 和 `ballsPlugin` 合并为统一的 `physicsPlugin`，并采用类似 `api-demo` 的模块化文件结构（`types.ts`、`state.ts`、`dvd.ts`、`balls.ts`、`utils.ts`），提高可维护性和可扩展性。
- **增强物理模拟**：
  - 小球质量与大小（半径平方）成正比，实现基于质量的真实弹性碰撞。
  - DVD 改为“不可阻挡”物体：小球与 DVD 碰撞时，DVD 速度保持不变（方向和大小均不变），小球按法向反射。
  - 完善边缘处理：分离重叠、边界检测。
- **修复 ticker 回调未正确添加的问题**：在 `state.ts` 中将 `tickerCallback` 初始值设为 `undefined`，并在 `index.ts` 中增加 `ensureTicker` 和 `removeTicker` 逻辑，确保动画真正运行。

### 困难与修正过程

#### 困难 1：业务逻辑与 UI 耦合

- **问题**：早期版本中，`App.tsx` 既负责渲染，又负责处理事件、转发鼠标消息、构造绘图指令，导致组件臃肿且难以测试。
- **尝试**：考虑将事件处理移到自定义 Hook 中，但仍需在组件中调用。
- **解决**：引入 `GameController` 类，它接收 `PixiController` 实例和一个日志回调，在构造函数中设置 `onMessageFromParent` 监听器，并公开绘图方法。`App.tsx` 只需实例化 `GameController` 并调用其方法，所有业务逻辑集中管理。

#### 困难 2：物理插件功能混杂

- **问题**：DVD 和小球的逻辑分散在两个插件中，且需要互相碰撞，导致状态同步复杂。
- **尝试**：在 `bouncePlugin` 和 `ballsPlugin` 之间通过消息通信，但增加了耦合。
- **解决**：将两者合并为 `physicsPlugin`，使用 `WeakMap` 统一管理状态（DVD 实体、小球数组、鼠标位置）。在 ticker 回调中统一更新所有实体并处理跨实体碰撞，代码清晰且性能良好。

#### 困难 3：小球静止不动（ticker 未启动）

- **问题**：首次实现 `physicsPlugin` 时，小球和 DVD 静止不动。
- **原因**：`state.ts` 中 `tickerCallback` 被初始化为一个空函数，导致 `ensureTicker` 误以为已存在，未添加真正的更新函数。
- **解决**：将初始值改为 `undefined`，并在 `index.ts` 中严格判断 `!state.tickerCallback` 时才创建并添加 ticker；同时在 `clear` 消息中调用 `removeTicker`，确保资源正确释放。

#### 困难 4：小球碰撞不真实

- **问题**：小球碰撞采用质量相等假设，无论大小如何，速度交换效果相同，不符合物理直觉。
- **解决**：在 `utils.ts` 的 `handleBallBallCollision` 中，根据半径计算质量（假设密度均匀，质量与半径平方成正比），使用完全弹性碰撞公式（基于动量守恒和能量守恒）更新速度，并按照质量比例分离重叠，实现大球更难被推动的效果。

#### 困难 5：DVD 应不可阻挡

- **问题**：原逻辑中小球与 DVD 碰撞时，DVD 会随机改变方向，与“不可阻挡”要求矛盾。
- **解决**：修改 `handleBallDVDCollision`，移除对 DVD 速度的修改，仅让小球法向反射，并移动小球避免重叠。DVD 自身仍保留边界反弹逻辑（随机变色、随机方向），但不受小球影响。

### 当前功能列表

- **PixiController**：管理插件注册、消息分发、事件回调。
- **PixiCanvas**：封装 Pixi 应用生命周期，捕获用户事件并抛出。
- **GameController**：业务逻辑层，处理事件、更新日志、转发鼠标消息，提供绘图 API。
- **插件系统**：
  - `clear.plugin.ts`：清空画布。
  - `circle.plugin.ts`：绘制圆形。
  - `rectangle.plugin.ts`：绘制矩形。
  - `fireworks.plugin.ts`：鼠标跟随烟花效果。
  - `apiDemoPlugin`（模块化）：PixiJS API 教学演示，包含 8 个子演示（基础图形、文本、精灵、动画、滤镜、交互、容器、粒子）。
  - **`physicsPlugin`（模块化）**：统一物理模拟，包含 DVD 动画和 100 个小球碰撞，支持质量、鼠标交互、边界反弹。
- **WebSocket 基础**：提供连接管理与消息接收（需手动桥接至控制器）。
- **测试界面**：按钮触发绘图、控制烟花效果、触发 API 演示、启动小球，实时显示事件日志，自动启动 DVD 动画。

### 测试验证

#### 测试 1：GameController 封装

- **操作**：点击画圆、画矩形等按钮。
- **预期结果**：画布上出现对应图形，日志显示指令发送。
- **实际结果**：✅ 通过。

#### 测试 2：物理插件启动与停止

- **操作**：启动应用，观察 DVD 自动运行；点击“启动100个小球”，小球出现；点击“清除”，所有图形消失。
- **预期结果**：DVD 移动，小球运动，清除后停止。
- **实际结果**：✅ 通过。

#### 测试 3：小球质量与大小关联

- **操作**：观察大小不同的小球碰撞。
- **预期结果**：大球移动幅度小，小球被弹开更远。
- **实际结果**：✅ 通过。

#### 测试 4：DVD 不可阻挡

- **操作**：让小球与 DVD 碰撞。
- **预期结果**：DVD 速度方向不变，小球被弹开。
- **实际结果**：✅ 通过。

#### 测试 5：鼠标撞击

- **操作**：移动鼠标。
- **预期结果**：小球被鼠标“撞开”，日志显示 `pointermove` 事件。
- **实际结果**：✅ 通过。

#### 测试 6：边界处理

- **操作**：观察小球和 DVD 运动到边界。
- **预期结果**：小球反弹减速（能量损失），DVD 反弹变色并随机转向。
- **实际结果**：✅ 通过。

#### 测试 7：API 演示共存

- **操作**：点击 API 演示按钮，再启动小球。
- **预期结果**：演示内容在左上角显示，小球在全画布运动，互不干扰。
- **实际结果**：✅ 通过。

#### 🗑️ 待移除内容

- **`src/plugins/bounce.plugin.ts` 和 `src/plugins/balls.plugin.ts`**：已由 `physicsPlugin` 替代，可删除。
- **`src/components/TestJSX.jsx`**：计划在下一个版本中删除。

---


## [0.6.0] - 2025-03-01

### 核心更新

- **新增 API 教学演示插件 (`apiDemoPlugin`)**：提供一系列从简单到复杂的 PixiJS v8 API 示例，涵盖基础图形、文本、精灵、动画、滤镜、交互、容器和粒子系统。每个示例都带有标题和说明文字，固定在画布左上角展示。
- **插件代码拆分**：将 `apiDemoPlugin` 拆分为多文件结构（`types.ts`、`state.ts`、`utils.ts` 及独立演示模块），提高可维护性和可扩展性。
- **修复清除后 API 演示失效的问题**：API 演示插件现在监听 `clear` 消息，收到后主动销毁自己的容器并重置状态，确保清除后再次点击按钮能正常显示新内容。
- **粒子演示适配 PixiJS v8**：修正 `ParticleContainer` 的构造函数用法，添加类型断言以兼容 v8 的实际 API。

### 困难与修正过程

#### 困难 1：清除后 API 演示按钮失效

- **问题**：点击“清除”按钮后，`clear.plugin.ts` 执行 `app.stage.removeChildren()` 移除了舞台上所有子对象，但 API 演示插件的容器虽然从舞台移除，仍保留在 `WeakMap` 状态中。后续点击 API 按钮时，`getDemoContainer` 返回已被移除的容器，导致新内容添加到不可见的容器上，画布无显示。
- **尝试**：
  - 最初考虑在 `clear.plugin` 中通知所有插件清理，但插件之间不应直接耦合。
  - 尝试在 `getDemoContainer` 中检查容器是否仍在舞台上，若不在则销毁重建，但此方法不够通用，且需要处理动画和交互对象的清理。
- **解决**：让 API 演示插件自身监听 `clear` 消息。在 `execute` 中增加对 `'clear'` 类型的处理，调用 `destroyDemoContainer` 函数（封装在 `state.ts` 中），该函数停止所有动画、销毁容器并从 `WeakMap` 中移除状态。这样清除后，下次点击 API 按钮时会重新创建全新的容器，问题解决。

#### 困难 2：ParticleContainer 在 PixiJS v8 中的类型错误

- **问题**：创建 `ParticleContainer` 时，TypeScript 提示 `capacity` 不是有效属性，因为 v8 的类型定义要求使用选项对象 `{ maxSize, properties }` 但实际运行时仍支持两个参数的形式。
- **尝试**：查阅官方文档，发现 v8 推荐使用选项对象，但为了保持代码简洁并确保运行时正确，决定保留两个参数形式并添加 `as any` 类型断言。
- **解决**：在 `particles.ts` 中使用 `new PIXI.ParticleContainer(1000, { ... } as any)`，并在注释中说明原因，确保代码既通过类型检查又能正常运行。

#### 困难 3：如何设计可扩展的演示插件结构

- **问题**：API 演示插件包含多个独立示例，如果所有代码写在一个文件里将难以维护。
- **解决**：采用模块化设计，将状态管理、工具函数、每个演示分别放在独立文件中，通过 `demos/index.ts` 导出映射表。主插件入口根据消息类型自动调用对应的演示函数，新增演示只需添加新文件并在映射表中注册，无需修改核心逻辑。

### 当前功能列表

- **PixiController**：管理插件注册、消息分发、事件回调。
- **PixiCanvas**：封装 Pixi 应用生命周期，捕获用户事件并抛出。
- **插件系统**：
  - `clear.plugin.ts`：清空画布。
  - `circle.plugin.ts`：绘制圆形。
  - `rectangle.plugin.ts`：绘制矩形。
  - `bounce.plugin.ts`：DVD 反弹动画，每次撞击随机变色、随机方向。
  - `fireworks.plugin.ts`：鼠标跟随烟花效果。
  - **`apiDemo.plugin.ts`（拆分后为 `api-demo/` 目录）**：PixiJS API 教学演示，包含 8 个子演示（基础图形、文本、精灵、动画、滤镜、交互、容器、粒子）。
- **WebSocket 基础**：提供连接管理与消息接收（需手动桥接至控制器）。
- **测试界面**：按钮触发绘图、控制烟花效果、触发 API 演示，实时显示事件日志，自动启动 DVD 动画。

### 测试验证

#### 测试 1：API 演示各功能

- **操作**：分别点击八个 API 演示按钮。
- **预期结果**：画布左上角依次显示对应的演示内容，包含正确的标题和说明，所有动画、交互正常工作。
- **实际结果**：✅ 通过。

#### 测试 2：清除后重新触发 API 演示

- **操作**：点击任意 API 演示按钮（如“基础图形”），然后点击“清除”按钮，再次点击同一 API 按钮。
- **预期结果**：清除后画布空白，再次点击后演示内容重新出现，控制台无报错。
- **实际结果**：✅ 通过。

#### 测试 3：粒子演示性能与正确性

- **操作**：点击“粒子”演示，观察 200 个彩色粒子在区域内缓慢移动、旋转、边界环绕。
- **预期结果**：粒子数量稳定，动画流畅，无性能问题。
- **实际结果**：✅ 通过。

#### 测试 4：与其他插件共存

- **操作**：启动后观察 DVD 动画自动运行，移动鼠标触发烟花，点击 API 演示按钮，再点击画圆等按钮。
- **预期结果**：所有插件互不干扰，API 演示容器独立于其他图形。
- **实际结果**：✅ 通过。

#### 🗑️ 待移除内容

- **`src/components/TestJSX.jsx`**：计划在下一个版本中删除（仍保留但建议移除）。

---
## [0.5.0] - 2025-02-28

### 核心更新

- **新增鼠标跟随烟花效果**：通过 `fireworks.plugin.ts` 插件实现，启动后在画布上移动鼠标时，鼠标位置不断生成彩色粒子，粒子向外扩散并逐渐淡出消失，营造烟花尾迹效果。
- **修复严格模式下 PixiCanvas 重复销毁错误**：引入 `safeDestroy` 函数和 `Symbol` 标记，确保每个 `PIXI.Application` 实例只被销毁一次，彻底消除控制台中的 `Cannot read properties of undefined` 错误。
- **优化插件注册方式**：修改 `src/plugins/index.ts`，将所有插件整合为 `plugins` 数组并导出。`App.tsx` 现在只需循环注册该数组，新增插件只需修改 `index.ts` 一处，无需改动 `App.tsx` 主体。
- **文档同步**：更新 VERSION.md 以反映新特性和修复。

---

## [0.4.0] - 2025-02-27

### 核心更新

- **增强反弹插件**：`bounce.plugin.ts` 升级至 v1.1.0，实现撞击时随机变色与随机方向。
- **交互趣味性**：每次碰撞边界，DVD标志随机改变颜色（通过 `sprite.tint`）且运动方向完全随机（速度大小不变），使动画更富变化。
- **文档同步**：更新 README.md 和 VERSION.md 以反映新特性。

---

## [0.3.0] - 2025-02-27

### 核心更新

- **新增 DVD 屏保风格反弹动画**：通过 `bounce.plugin.ts` 插件实现，画布初始化后自动启动一个彩色“DVD”标志，在画布内匀速移动并边界反弹。
- **插件支持持续性动画**：首次在插件中引入状态管理（使用 `WeakMap` 存储每个 `app` 实例的精灵、速度、ticker 回调），并利用 `PIXI.Ticker` 实现连续更新。
- **插件间协作**：`bouncePlugin` 同时监听 `clear` 消息，在画布被清除时主动停止动画、销毁精灵，避免资源泄露和无效更新。
- **新增 SVG 资源**：在 `src/assets/dvd-logo.svg` 中添加测试用的 DVD 标志图片。
- **更新测试界面**：`App.tsx` 在 `handleAppInit` 中自动发送 `startDVD` 消息，日志记录启动动作。

---

## [0.2.0] - 2025-02-27

### 核心更新

- **完全模块化的插件系统**：所有绘图指令从 `PixiCanvas` 组件中剥离，拆分为独立插件（`clear.plugin.ts`、`circle.plugin.ts`、`rectangle.plugin.ts`）。
- **标准化插件接口**：定义 `PixiPlugin` 类型（`messageTypes` + `execute`），控制器按消息类型分发。
- **WebSocket 服务基础**：创建 `websocket.service.ts` 和 `useWebsocket.ts`，提供 WebSocket 连接管理与消息接收能力。
- **双向通信增强**：`PixiController` 支持 `sendToParent` 和 `onMessageFromPixi`，实现 React 组件与 Pixi 画布的事件互传。
- **错误处理机制**：控制器捕获插件执行中的异常，并通过 `sendToParent` 上报错误，避免应用崩溃。
- **测试界面优化**：`App.tsx` 显示事件日志，集成错误边界组件。

---

## [0.1.0] - 2025-02-20

### 初始功能

- 搭建 React + TypeScript + PixiJS v8 项目基础结构。
- 创建 `PixiController` 作为简单消息中枢，支持注册单一插件（函数式）。
- 创建 `PixiCanvas` 组件，实现 Pixi 应用的初始化和销毁。
- 在 `App.tsx` 中提供测试按钮，直接调用硬编码的绘图函数（`drawCircle`、`drawRectangle`、`clear`）。
- 点击画布时在点击位置绘制红色点，并随机生成蓝色点（逻辑混合在组件中）。

---

## 版本对比摘要

| 版本  | 插件系统       | 事件通信              | WebSocket  | 错误处理 | 代码维护性 |
| ----- | -------------- | --------------------- | ---------- | -------- | ---------- |
| 0.1.0 | 硬编码         | 单向（React -> Pixi） | 无         | 无       | 低         |
| 0.2.0 | 模块化、接口化 | 双向                  | 基础服务层 | 有       | 高         |
| 0.3.0 | + 持续性动画   | 双向                  | 基础服务层 | 有       | 高         |
| 0.4.0 | + 增强反弹效果 | 双向                  | 基础服务层 | 有       | 高         |
| 0.5.0 | + 烟花效果     | 双向                  | 基础服务层 | 有       | 高         |
| 0.6.0 | + API 教学演示 | 双向                  | 基础服务层 | 有       | 高         |
| 0.7.0 | + 统一物理插件、GameController、基于质量的碰撞 | 双向 | 基础服务层 | 有 | 高 |